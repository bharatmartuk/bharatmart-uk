node_modules
.next
.turbo
dist
.env*.local
.env
*.tsbuildinfo
.DS_Store
packages/database/generated
/uploads

# Local media source files — never commit (keep the repo light).
# Product images / merchant logos are stored in Cloudinary; only URLs live in the database.
assets/
stitch_bharatmart_uk_design_system/
